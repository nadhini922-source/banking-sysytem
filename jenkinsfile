pipeline {
    agent any

    stages {
        stage('Hello World') {
            steps {
                bat 'python hello.py'
            }
        }
    }

    post {
        success {
            echo 'Build Successful!'
        }
        failure {
            echo 'Build Failed!'
        }
        always {
            echo 'Pipeline Finished!'
        }
    }
}